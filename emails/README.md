# Email Templates

This directory contains HTML templates for transactional emails.

## Placeholders

Templates use double-curly placeholders, for example:

- `{{tripTitle}}`
- `{{tripUrl}}`
- `{{recipientEmail}}`

These placeholders should be replaced in the email sending layer before dispatch.
